///without this, you woll rely on nextjs default behavior
///you cannot add custom webpack, optimize images, use env variables etc.
/** @type {import('next').NextConfig} */ 
///sets up the nextConfig object, without this no custom configurations
const nextConfig = {};

///making the nextConfig object default export
export default nextConfig;
